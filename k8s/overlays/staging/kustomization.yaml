apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitops-staging

namePrefix: staging-

resources:
- ../../base

patchesStrategicMerge:
- deployment-patch.yaml
- quarkus-deployment-patch.yaml

# Note: commonLabels removed to avoid mutating deployment selectors
# Environment labels are added via patches instead

images:
- name: gitops-demo
  newName: ghcr.io/vermasuraj10678/gitops/gitops-demo
  newTag: main-5a86f24

